name: hello world workflow

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: first strip
        run: echo "hello world"
        shell: bash
